fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2

# Added for the ML energy-prediction module (ai-optimizers/ml). Kept to the
# minimum needed to load a pre-trained scikit-learn Pipeline and run
# inference -- training itself (train.py) is run offline/locally, not in
# the deployed backend.
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.1.3
joblib==1.4.2
requests==2.32.3
