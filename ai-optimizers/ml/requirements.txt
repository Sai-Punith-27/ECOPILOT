# Requirements to train or run ai-optimizers/ml standalone (outside the
# backend). Kept in sync with the ML-related pins in
# backend/backend/requirements.txt -- the backend only needs these to load
# and run the already-trained model (see app/ml_bridge.py); this file is
# for anyone re-running train.py locally.
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.1.3
joblib==1.4.2
pytest==8.3.3
